# Notes

Short markdown file inside a multi-format folder.

This tests that the **markdown renderer** works correctly even when it's not the entry file — the tab switches from HTML to this file on click.

## Checklist

- [ ] Tab dot is blue (`.md` color)
- [ ] Content renders as HTML, not raw text
- [ ] No `##` symbols visible — parsed as heading
